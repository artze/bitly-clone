<div class="container">
	<div class="form">
		<form action="/urls" method="POST"> 
		    <div class="row justify-content-center">
			    <div class="col-6">
			    		<input type="text" name="long_url" class="form-control" placeholder="Enter URL here">
			    </div>
		    <input type="submit" value="shortn" class="btn btn-color">
		    </div>
		</form>
	</div>

	<div class="row justify-content-center">
		<div class="col-4">
				<% if @error_messages %>
					<% @error_messages.messages[:link].each do |msg|%>
					<p class="error-msg">
					<%= msg %>
					</p>
					<%end%>
				<% end %>
		</div>
	</div>
</div>

<div class="container">
	<div class="url-list row justify-content-center">
		<% unless Url.all.empty? %>
			<table align="center">
				<col width="260">
				<col width="260">
				<col width="100">
				<tr>
					<th height="30">URL</th>
					<th height="30">Shortn URL</th>
					<th height="30">Click Count</th>
				</tr>
				<% Url.all.order(click_count: :desc).each do |result| %>
				<tr>
					<td><%=	result.link %></td>
					<td><a href="<%= 'http://localhost:9393/' + result.serial_code %>"><%= 'http://localhost:9393/' + result.serial_code %></a></td>
					<td><%=	result.click_count %></td>
				</tr>
				<% end %>
			</table>
		<% end %>
	</div>
</div>